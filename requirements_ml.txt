# ============================================================
# ML Engine Requirements — Crypto Trading Bot v2.0
# ============================================================
# Install: pip install -r requirements_ml.txt
#
# NOTE: TA-Lib on Windows requires pre-compiled binary.
# The setup_ml.bat script handles this automatically.
# ============================================================

# ── Core ML ──────────────────────────────────────────────────
tensorflow>=2.15.0
keras>=2.15.0
xgboost>=2.0.3
scikit-learn>=1.4.0
optuna>=3.5.0
mlflow>=2.11.0

# ── Reinforcement Learning ────────────────────────────────────
stable-baselines3>=2.3.0
gymnasium>=0.29.0
shimmy>=1.3.0

# ── Technical Analysis ────────────────────────────────────────
# TA-Lib is installed separately via .whl (see setup_ml.bat)
pandas-ta>=0.3.14b0
ta>=0.11.0

# ── Data Fetching ─────────────────────────────────────────────
ccxt>=4.2.0
yfinance>=0.2.37
aiohttp>=3.9.0

# ── Database ─────────────────────────────────────────────────
SQLAlchemy>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pymongo>=4.6.0
dnspython>=2.6.0

# ── Data Processing ──────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=15.0.0
fastparquet>=2024.2.0

# ── LLM (Gemini) — replaces Anthropic ────────────────────────
google-generativeai>=0.5.0

# ── Existing Backend Dependencies (keep) ─────────────────────
fastapi>=0.109.0
uvicorn>=0.27.0
python-telegram-bot>=20.0
pydantic>=2.5.0
python-dotenv>=1.0.0
aiofiles>=23.2.0
requests>=2.31.0
httpx>=0.26.0
